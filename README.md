# n8n Deployment

Este proyecto levanta una instancia de n8n usando Docker y PostgreSQL.

## Requisitos
- Docker y Docker Compose
- Archivo `.env` basado en las variables del `docker-compose.yml`

## Instalación
1. Clonar el repo.
2. Crear archivo `.env` con las credenciales.
3. Ejecutar `docker-compose up -d`.